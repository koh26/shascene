<%= @city.name %>

<% @post.each do |post| %>
<%= link_to image_tag(post.image.variant(resize: '600x600')),map_post_path(@city, post) %>
<%= post.title %>
<%= post.about %>
<%= post.day %>
<%= post.town %>
<%= post.number %>
<%= post.building %>
<% end %>

<%= link_to "投稿", new_map_post_path(@city), class: :nav__btn %>